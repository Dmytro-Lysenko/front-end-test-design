import React from "react";
import CartList from "../components/Cart/CartList";

const Cart = (props) => {
  return (
    <div>
      <h1>CART</h1>
      <CartList />
    </div>
  );
};

export default Cart;
